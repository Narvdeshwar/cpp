#include <bits/stdc++.h>
using namespace std;
void TakingInputFromUser() { return 0; }

void PrintingArray(int arr[]) {
  for (int i = 0; i < 3; i++) {
    cout << arr[i] << " ";
  }
  return 0;
}
int main() {
  // # Creation of array
  int arr[10];
  // int brr[]; // erorr as size is not provided in the array(Error : storage
  // size of ‘brr’ isn’t known)
  int crr[3];
  fill(crr, crr + 3, 11); // filling the default value in the array we can do this by for loop also.

  return 0;
}